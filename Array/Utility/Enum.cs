namespace Skillup.Array.Utility
{
    /// <summary>
    /// enum for Sorting column name.
    /// </summary>
    public enum SortingColumn
    {
        X,
        Y,
    }

    /// <summary>
    /// enum for sorting order name and number.
    /// </summary>
    public enum SortingOrder
    {
        Ascending = 1,
        Descending = 2,
    }
}
